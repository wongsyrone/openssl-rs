[package]
name = "systest"
version = "0.1.0"
authors = ["Alex Crichton <alex@alexcrichton.com>"]
edition = "2018"

[dependencies]
libc = "0.2"
openssl-rs-sys = { path = "../openssl-rs-sys" }

[build-dependencies]
ctest2 = "0"

[features]
vendored = ['openssl-rs-sys/vendored']
